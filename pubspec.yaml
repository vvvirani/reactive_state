name: reactive_state
description: "A lightweight auto-tracking reactive state management library for Flutter."
version: 1.0.0
homepage: "https://github.com/vvvirani/reactive_state"

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
