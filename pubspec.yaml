name: reactive_flutter
description: "A lightweight auto-tracking reactive state management library for Flutter."
version: 1.0.2
homepage: "https://github.com/vvvirani/reactive_flutter"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
